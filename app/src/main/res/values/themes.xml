<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme -->
    <style name="Base.Theme.MenuBH" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Define a cor da barra de notificação -->
        <item name="android:statusBarColor">@color/headerFooter</item>
    </style>

    <!-- Derived theme -->
    <style name="Theme.MenuBH" parent="Base.Theme.MenuBH">
        <item name="android:statusBarColor">@color/headerFooter</item>
    </style>
</resources>
